<p id="notice"><%= notice %></p>

  <div class="col-sm-8">
    <p>
      <strong>Title:</strong>
      <%= @post.title %>
    </p>

    <p>
      <strong>Body:</strong>
      <%= @post.body %>
    </p>
    <%= link_to 'Back', posts_path, {class: "btn btn-primary btn-sm"}%> |
    <%= link_to 'Edit Post', edit_post_path, {class: "btn btn-warning btn-sm"} %>
  </div>
</div>
